# ~/.p10k.zsh is deliberately mutable and is seeded from this repository's
# configuration only when absent. This allows `p10k configure` and manual edits
# to persist across Home Manager switches.
[[ ! -r "$HOME/.p10k.zsh" ]] || source "$HOME/.p10k.zsh"
